apiVersion: v1
kind: Service
metadata:
  name: {{ include "check-in-bot.fullname" . }}
  labels:
    {{- include "check-in-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "check-in-bot.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
